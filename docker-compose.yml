services:
  app:
    image: glyphr-studio:v2.5.2
    build:
      context: .
      dockerfile: Dockerfile
    container_name: glyphr-studio-container
    ports:
     - 3000:80
    environment:
      - TZ=Asia/Ho_Chi_Minh
    restart: unless-stopped # always, on-failure, unless-stopped
